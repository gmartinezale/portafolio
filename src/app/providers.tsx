'use client';

// HeroUI v3 does not require a provider wrapper.
// This component is kept as a structural placeholder.
export default function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
